<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.olgacoll.airmns">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_action_menu"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_action_info"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar" >
        <activity
            android:name=".Activity0SplashScreen"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Activity1LoginActivity"/>
        <activity android:name=".Activity2SignupActivity" />
        <activity android:name=".Activity3AMainUser" />
        <activity android:name=".Activity3BMainProfessional"/>
        <activity android:name=".Activity4EditProfile"/>
        <activity android:name=".Activity5Reserve"/>

        <!-- TODO Activities 6-8 -->
        <activity android:name=".Activity9InfoActivity"/>
    </application>
</manifest>